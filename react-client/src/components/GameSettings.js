import React, { Component } from "react";

class GameSettings extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
        ToDo
      </div>
    );
  }
}

export default GameSettings;